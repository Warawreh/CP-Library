#include <bits/stdc++.h>

using namespace std;

#define all(x) (x).begin(), (x).end()
typedef long long ll;
typedef long double ld;
typedef unsigned long long ull;

int main(){
	string s;
	cin>>s;
	if(s.size() == 10 && s[0] == '0' && s[1] == '1')cout << "yes\n";
	else cout << "no\n";
}